def + = llvm { a: Int, b: Int ->
  %1 = add nsw i32 %other, %self
  ret i32 %1
}
def main = { a: Int, b: Int, c: Int ->
    (1 + 1)
    (1 + 0) + 3 + 4.times * (3 + 1).shl - a.b.c.bar(baz()) + foo(1,1)
    xor(1 + 1, 1)
    a xor b
}