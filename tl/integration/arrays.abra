import abra.int
import abra.arrays

def main = {
  val array = arrays.mkIntArray(100, \i -> i + 1)
  var i = 0
  var sum = 0

  while i < array.length {
    sum = sum + array(i)
    i = i + 1
  }

  array.free
  sum
}: Int