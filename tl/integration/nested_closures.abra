def +: (self: Int, other: Int) -> Int = llvm {
  %1 = add nsw i32 %self, %other
  ret i32 %1
}

def main = {
    var x = 0

    { i: Int ->
        val y = i

        { j: Int ->
            { k: Int ->
                x = x + y + i + j + k
            }(3)
        }(2)
    }(1)

    x
}: Int # 7 expected