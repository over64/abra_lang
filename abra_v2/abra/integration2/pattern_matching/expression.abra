def == = \self: Int, other: Int -> llvm
  %1 = icmp eq i32 %self, %other
  ret i1 %1
.Boolean

def + = \self: Int, other: Int -> llvm
  %1 = add nsw i32 %self, %other
  ret i32 %1
.Int

def main =
    match 2
        of $(1 + 1) -> 3 ..