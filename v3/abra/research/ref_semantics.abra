let x = 1
let y = x
y = 2 # y = 2, x = 1

let x = Vec2(1, 2)
let y = x
y.x = 2 # x same x where Vec2(2, 2)

let x = array.of(
    Vec2(1, 2),
    Vec2(2, 3)) # value

let x0 = x(0) # ref
let y0 = x0 # ref
let z = array.of(x0, y0)

