1. Каталоги резолвятся через
  - current directory
  - home local (like .m2)
  - external(net) resolver - https://abra-pkg.org/pkg_name.tar.gz
2. Резолверы можно добавлять свои в опред порядке
3. require требует указания через флаг компилятора (e.g. -DOS=windows)
4. require переменные потом могут быть использованы в именах импортируемых файлов

require OS of windows, linux, mac, android
import libusb@1.0.0/$OS/${OS}device
import abra@1.8.33/io