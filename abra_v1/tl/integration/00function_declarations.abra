# ЧТО ИМЯ: [ТИП] = ТЕЛО
def foo = 1 # foo: () -> Int
def foo2: () -> Int = 1

def bar2 = { x: Int -> x }: Int
def bar3 = \x: Int -> x # ???
def bar4: (x: Int) -> Int = \x -> x


def baz: (a: Int) -> Int = llvm {
    ret i32 %a
}
def main = {
  1
}: Int