import abra.io with Fd

type LinesIterator = (fd: Fd) # rewrites to io.Fd

def mkLinesIterator = \path: String -> LinesIterator(io.open(path))
def nextLine = \self: LinesIterator ->
    self.fd.readLine()

def dup: (self: Fd) -> Fd = llvm {
}
